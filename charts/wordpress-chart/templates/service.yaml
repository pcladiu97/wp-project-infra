apiVersion: v1
kind: Service
metadata:
  name: wordpress
spec:
  type: {{ .Values.service.type }}
  selector:
    app: wordpress
  ports:
  {{- range .Values.service.ports }}
    - name: {{ .name }}
      port: {{ .port }} 
      targetPort: {{ .targetPort }}
  {{- end }}